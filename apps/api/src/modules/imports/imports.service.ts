import { BadRequestException, Injectable } from "@nestjs/common"; import ExcelJS from "exceljs"; import { randomUUID } from "node:crypto"; import { batchImportRowSchema } from "@buyhksim/contracts"; import { IdempotencyService } from "../../shared/idempotency.service";
type ImportResult={jobId:string;totalRows:number;validRows:number;errorRows:number;errors:{row:number;field:string;message:string}[]};
@Injectable() export class ImportsService { private lastJob:ImportResult|null=null;constructor(private readonly idem:IdempotencyService){} async validate(file?:Express.Multer.File){if(!file)throw new BadRequestException("缺少导入文件");const isCsv=file.originalname.toLowerCase().endsWith(".csv");const isXlsx=file.originalname.toLowerCase().endsWith(".xlsx");if(!isCsv&&!isXlsx)throw new BadRequestException("仅支持 CSV 或 Excel 文件");if(isXlsx&&!file.buffer.subarray(0,4).equals(Buffer.from([0x50,0x4b,0x03,0x04])))throw new BadRequestException("Excel 文件签名不正确");if(isCsv&&file.buffer.includes(0))throw new BadRequestException("CSV 文件包含非法二进制内容");const workbook=new ExcelJS.Workbook();if(isCsv)await workbook.csv.read(file.buffer as never);else await workbook.xlsx.load(file.buffer as never);const sheet=workbook.worksheets[0];if(!sheet)throw new BadRequestException("文件中没有工作表");if(sheet.rowCount>10_001)throw new BadRequestException("单次导入不得超过 10,000 行");const headers=(sheet.getRow(1).values as unknown[]).slice(1).map((value)=>String(value).trim());const required=["externalId","title","skuCode","destination","kind","validityDays","priceMinor","stock"];const missing=required.filter((header)=>!headers.includes(header));if(missing.length)throw new BadRequestException(`缺少必要列：${missing.join("、")}`);if(new Set(headers).size!==headers.length)throw new BadRequestException("导入文件包含重复列名");const errors:ImportResult["errors"]=[];let validRows=0;sheet.eachRow((row,rowNumber)=>{if(rowNumber===1)return;const raw=Object.fromEntries(headers.map((h,index)=>[h,row.getCell(index+1).value]));const parsed=batchImportRowSchema.safeParse({externalId:String(raw.externalId??""),title:String(raw.title??""),skuCode:String(raw.skuCode??""),destination:String(raw.destination??""),kind:String(raw.kind??""),dataGb:raw.dataGb===""||raw.dataGb==null?null:Number(raw.dataGb),validityDays:Number(raw.validityDays),priceMinor:Number(raw.priceMinor),stock:Number(raw.stock),commissionBps:Number(raw.commissionBps??0)});if(parsed.success)validRows++;else for(const issue of parsed.error.issues)errors.push({row:rowNumber,field:issue.path.join("."),message:issue.message})});this.lastJob={jobId:randomUUID(),totalRows:Math.max(0,sheet.rowCount-1),validRows,errorRows:new Set(errors.map((e)=>e.row)).size,errors:errors.slice(0,1_000)};return {ok:true,data:this.lastJob,requestId:randomUUID()}} async commit(key:string){if(!this.lastJob)throw new BadRequestException("请先上传并校验文件");const job=this.lastJob;return this.idem.execute("product-import",key,{jobId:job.jobId},async()=>({ok:true,data:{jobId:job.jobId,status:"QUEUED",acceptedRows:job.validRows},requestId:randomUUID()}))} }
