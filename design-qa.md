# BUYHKSIM V2 设计 QA

视觉基准：`docs/design/selected-homepage-option-2.png`

实现地址：消费者商城端口 3100，商家后台端口 3101（本地进程，2026-08-01 已确认均返回 HTTP 200）。

## 已完成的静态检查

- 品牌、信息架构、象牙白/海军蓝/森林绿/琥珀价格色与所选方案一致。
- 使用真实生成的罗马主视觉和香港、日本、欧洲、新加坡、泰国商品图片。
- 页面在 360、768、1024、1440 宽度设置了响应式布局；移动端包含底部导航和商品页固定购买栏。
- 所有主要导航、搜索、筛选、主题、语言、收藏、购物车、结算、订单、商品创建和批量导入控件均有可执行行为。
- Lint、TypeScript、单元测试和生产构建通过。
- 统一增加最小宽度、文本换行、图标不收缩、按钮行高与移动端头部断点，针对字体、按钮文字和图标重叠完成静态修复。
- 360px 下顶部公告自动隐藏，主题与语言选择器限制宽度；后台新增移动抽屉导航，表格使用横向滚动避免挤压。

## 阻塞项

当前应用内浏览器连接组件报错，无法按 Product Design 规范完成“参考图 + 同视口实现截图”的组合对比，也无法执行最终视觉回归和真实点击巡检。已向用户请求授权改用独立 Playwright；在获得授权前不越权使用。

final result: blocked — 等待用户授权使用独立 Playwright 完成视觉与交互 QA
